
---
import '../styles/global.css';
import { getUserFromCookie } from '../lib/auth';
import User from '../models/User';
import mongoose from 'mongoose';

interface Props {
  slug: string;
}

const { slug } = Astro.props;

// Get current user
const cookieHeader = Astro.request.headers.get('cookie');
const userPayload = getUserFromCookie(cookieHeader);
let currentUser = null;

if (userPayload) {
  try {
     if (mongoose.connection.readyState !== 1) {
        const MONGODB_URI = import.meta.env.MONGODB_URI;
        if (MONGODB_URI) await mongoose.connect(MONGODB_URI);
     }
     const userDoc = await User.findById(userPayload.userId);
     if (userDoc) {
       currentUser = {
         id: userDoc._id.toString(),
         name: userDoc.name,
         email: userDoc.email,
         picture: userDoc.picture
       };
     }
  } catch (e) {
    console.error('Error fetching user for comments:', e);
  }
}
const isLoggedIn = !!currentUser;
---

<div class="comments-layout">
  
  <!-- Sidebar: Sticky Form Area -->
  <aside class="comments-sidebar">
    <div class="sticky-content">
      <div class="sidebar-header">
        <h3 class="gradient-text">Comentarios</h3>
        <div class="live-indicator">
          <span class="pulse"></span>
          <span id="comment-count-label">0 comentarios</span>
        </div>
      </div>
      
      <p class="sidebar-desc">
        Únete al debate. Comparte tu opinión sobre este episodio con la comunidad.
      </p>

      <form id="comment-form" class="modern-form">
        {isLoggedIn ? (
          <div class="logged-in-badge">
            <div class="badge-avatar">
              {currentUser?.picture ? (
                <img src={currentUser.picture} alt={currentUser.name} />
              ) : (
                <div class="avatar-placeholder">{currentUser?.name.charAt(0).toUpperCase()}</div>
              )}
            </div>
            <div class="badge-info">
              <span class="badge-label">Comentando como</span>
              <strong class="badge-name">{currentUser?.name}</strong>
            </div>
            <input type="hidden" name="name" value={currentUser?.name} />
            <input type="hidden" name="email" value={currentUser?.email} />
          </div>
        ) : (
          <div class="form-grid">
            <div class="input-wrapper">
               <input type="text" id="name" name="name" placeholder=" " required maxlength="60" />
               <label for="name">Nombre</label>
               <div class="focus-line"></div>
            </div>
            
            <div class="input-wrapper">
               <input type="email" id="email" name="email" placeholder=" " required />
               <label for="email">Email (privado)</label>
               <div class="focus-line"></div>
            </div>
          </div>
        )}

        <div class="input-wrapper full-width">
          <textarea id="text" name="text" rows="3" placeholder=" " required maxlength="1000"></textarea>
          <label for="text">Tu mensaje...</label>
          <div class="focus-line"></div>
        </div>

        <!-- Star Rating -->
        <div class="rating-wrapper">
          <span class="rating-label">¿Qué te ha parecido este episodio?</span>
          <div class="star-rating" id="star-rating">
            <button type="button" class="star" data-value="1" aria-label="1 estrella">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            </button>
            <button type="button" class="star" data-value="2" aria-label="2 estrellas">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            </button>
            <button type="button" class="star" data-value="3" aria-label="3 estrellas">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            </button>
            <button type="button" class="star" data-value="4" aria-label="4 estrellas">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            </button>
            <button type="button" class="star" data-value="5" aria-label="5 estrellas">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            </button>
          </div>
          <input type="hidden" name="rating" id="rating-value" value="0" />
        </div>

        <div class="form-footer">
          <button type="submit" class="submit-btn">
            <span>Publicar</span>
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"></line><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg>
          </button>
          <p id="form-status" class="status-msg"></p>
        </div>
      </form>
    </div>
  </aside>

  <!-- Main Feed: Scrollable Comments -->
  <div class="comments-feed" id="comments-container">
    <div id="comments-list" class="feed-list">
      <div class="loading-state">
        <div class="loader-line"></div>
        <p>Cargando comentarios...</p>
      </div>
    </div>
  </div>

  <!-- Toast Container -->
  <div id="toast-container" class="toast-container"></div>

</div>

<script define:vars={{ slug }}>
  const form = document.getElementById('comment-form');
  const list = document.getElementById('comments-list');
  const countLabel = document.getElementById('comment-count-label');
  const toastContainer = document.getElementById('toast-container');

  // --- TOAST SYSTEM ---
  function showToast(message, type = 'info') {
      const toast = document.createElement('div');
      toast.className = `toast toast-${type}`;
      
      const icon = type === 'success' ? '✅' : type === 'error' ? '❌' : 'ℹ️';
      
      toast.innerHTML = `
          <div class="toast-content">
              <span class="toast-icon">${icon}</span>
              <p class="toast-message">${message}</p>
          </div>
      `;
      
      toastContainer.appendChild(toast);
      
      // Animate in
      requestAnimationFrame(() => {
          toast.style.opacity = '1';
          toast.style.transform = 'translateY(0)';
      });
      
      // Remove after 4s
      setTimeout(() => {
          toast.style.opacity = '0';
          toast.style.transform = 'translateY(10px)';
          setTimeout(() => toast.remove(), 300);
      }, 4000);
  }

  async function fetchComments() {
    try {
      const res = await fetch(`/api/comments/${slug}`);
      const json = await res.json();
      
      if (json.success) {
        renderComments(json.data);
      } else {
        showToast('Error cargando comentarios', 'error');
      }
    } catch (e) {
       // Silent fail on load or show toast if critical
    }
  }

  function renderComments(comments) {
    if (comments.length === 0) {
      list.innerHTML = `
        <div class="empty-feed">
          <div class="empty-icon">💬</div>
          <h4>Aún no hay comentarios</h4>
          <p>Sé la primera persona en compartir sus pensamientos.</p>
        </div>
      `;
      countLabel.innerText = '0 comentarios';
      return;
    }

    countLabel.innerText = `${comments.length} comentario${comments.length !== 1 ? 's' : ''}`;
    
    list.innerHTML = comments.map(c => `
      <article class="feed-item" id="comment-${c._id}">
        <div class="item-avatar">
            <img src="${c.avatar}" alt="${escapeHtml(c.name)}" loading="lazy" />
        </div>
        <div class="item-content">
            <header class="item-header">
                <div class="author-row">
                    <span class="item-author">${escapeHtml(c.name)}</span>
                    ${c.rating > 0 ? `<div class="inline-rating">${renderStars(c.rating)}</div>` : ''}
                </div>
                <div class="header-right">
                    <time class="item-date">${timeAgo(new Date(c.createdAt))}</time>
                    <button class="delete-btn" title="Eliminar este comentario" onclick="requestDelete('${c._id}')">
                        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"></polyline><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path></svg>
                    </button>
                </div>
            </header>
            <div class="item-body">
                ${escapeHtml(c.text)}
            </div>
        </div>
      </article>
    `).join('');
  }

  // --- DELETE LOGIC ---
  window.requestDelete = async (commentId) => {
      // Direct action: verify via email is the safety mechanism
      const btn = document.querySelector(`#comment-${commentId} .delete-btn`);
      if(btn) btn.style.opacity = '0.5';

      try {
          // 1. Request Delete Token from Server
          const res = await fetch(`/api/comments/${slug}`, {
              method: 'DELETE',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ commentId })
          });
          const json = await res.json();
          
          if (json.success) {
              showToast('Enlace de confirmación enviado a tu correo.', 'info');
          } else {
              showToast(json.error || 'Error al solicitar el borrado.', 'error');
          }
      } catch (e) {
          showToast('Error de conexión.', 'error');
      } finally {
          if(btn) btn.style.opacity = '1';
      }
  };

  function escapeHtml(text) {
    if (!text) return '';
    return text.replace(/[&<>"']/g, function(m) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[m];
    });
  }

  function timeAgo(date) {
    const seconds = Math.floor((new Date() - date) / 1000);
    let interval = seconds / 31536000;
    if (interval > 1) return Math.floor(interval) + " años";
    interval = seconds / 2592000;
    if (interval > 1) return Math.floor(interval) + " meses";
    interval = seconds / 86400;
    if (interval > 1) return Math.floor(interval) + " días";
    interval = seconds / 3600;
    if (interval > 1) return Math.floor(interval) + " h";
    interval = seconds / 60;
    if (interval > 1) return Math.floor(interval) + " min";
    return "ahora mismo";
  }

  function renderStars(rating) {
    let stars = '';
    for (let i = 1; i <= 5; i++) {
      const filled = i <= rating;
      stars += `<span class="display-star ${filled ? 'filled' : ''}">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
      </span>`;
    }
    return stars;
  }

  // --- STAR RATING LOGIC ---
  const starRating = document.getElementById('star-rating');
  const ratingInput = document.getElementById('rating-value');
  let currentRating = 0;

  if (starRating && ratingInput) {
    const stars = starRating.querySelectorAll('.star');

    const updateStars = (value, isHover = false) => {
      stars.forEach((star, index) => {
        if (index < value) {
          star.classList.add(isHover ? 'hover' : 'active');
          if (!isHover) star.classList.remove('hover');
        } else {
          star.classList.remove(isHover ? 'hover' : 'active');
          if (!isHover) star.classList.remove('hover');
        }
      });
    };

    stars.forEach(star => {
      star.addEventListener('click', () => {
        currentRating = parseInt(star.dataset.value, 10);
        ratingInput.value = currentRating;
        updateStars(currentRating, false);
      });

      star.addEventListener('mouseenter', () => {
        updateStars(parseInt(star.dataset.value, 10), true);
      });
    });

    starRating.addEventListener('mouseleave', () => {
      stars.forEach(star => star.classList.remove('hover'));
      updateStars(currentRating, false);
    });
  }


  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const formData = new FormData(form);
    const data = {
      name: formData.get('name'),
      email: formData.get('email'),
      text: formData.get('text'),
      rating: parseInt(formData.get('rating') || '0', 10)
    };

    const btn = form.querySelector('button');
    if (btn) btn.disabled = true;
    
    const originalBtnText = btn.innerHTML;
    btn.innerHTML = '<span class="loader-dots">Enviando...</span>';

    try {
      // 1. Create Comment in DB (Server)
      const res = await fetch(`/api/comments/${slug}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
      });
      const json = await res.json();

      if (json.success) {
         form.reset();
         if (json.pending === false) {
           showToast('¡Comentario publicado!', 'success');
           // Reload comments to show the new one
           fetchComments();
         } else {
           showToast('¡Casi listo! Revisa tu email para verificar.', 'info');
         }
      } else {
         showToast(json.error || 'Error al publicar', 'error');
      }
    } catch (e) {
       showToast('Error de conexión', 'error');
    } finally {
      btn.disabled = false;
      btn.innerHTML = originalBtnText;
    }
  });

  fetchComments();
</script>

<style is:global>
  /* Layout Configuration */
  .comments-layout {
      display: flex;
      flex-direction: column;
      gap: 4rem;
      margin-top: 6rem;
      padding-top: 3rem;
      border-top: 1px solid rgba(255,255,255,0.05);
      font-family: var(--font-body);
      max-width: 700px; /* Optimal reading width */
      margin-left: auto;
      margin-right: auto;
  }

  /* SIDEBAR (Now Top Form) */
  .comments-sidebar {
      position: relative;
      width: 100%;
  }
  
  .sticky-content {
      position: relative;
      top: 0;
  }

  .sidebar-header {
      margin-bottom: 1rem;
  }

  .gradient-text {
      background: linear-gradient(135deg, #fff 0%, #a5a5a5 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-size: 2rem;
      font-weight: 800;
      font-family: var(--font-display);
      margin: 0;
      letter-spacing: -0.02em;
  }

  .live-indicator {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-top: 0.5rem;
      font-size: 0.85rem;
      color: var(--color-text-muted);
      font-weight: 500;
  }

  .pulse {
      width: 8px;
      height: 8px;
      background: #22c55e;
      border-radius: 50%;
      box-shadow: 0 0 0 rgba(34, 197, 94, 0.4);
      animation: pulse 2s infinite;
  }
  
  @keyframes pulse {
      0% { box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.4); }
      70% { box-shadow: 0 0 0 6px rgba(34, 197, 94, 0); }
      100% { box-shadow: 0 0 0 0 rgba(34, 197, 94, 0); }
  }

  .sidebar-desc {
      color: var(--color-text-dim);
      font-size: 0.95rem;
      line-height: 1.5;
      margin-bottom: 2rem;
      opacity: 0.8;
  }

  /* MODERN FORM STYLES */
  .modern-form {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
      padding: 0.5rem 0;
  }

  .form-grid {
      display: grid;
      gap: 1.5rem;
  }

  .input-wrapper {
      position: relative;
  }

  .input-wrapper input,
  .input-wrapper textarea {
      width: 100%;
      background: rgba(255,255,255,0.03);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 8px;
      padding: 1.25rem 1rem 0.5rem; /* Space for floating label */
      color: #fff;
      font-size: 0.95rem;
      transition: all 0.3s ease;
      font-family: inherit;
  }
  
  [data-theme="light"] .input-wrapper input,
  [data-theme="light"] .input-wrapper textarea {
      background: #fff;
      border-color: rgba(0,0,0,0.1);
      color: #000;
  }

  .input-wrapper label {
      position: absolute;
      left: 1rem;
      top: 1rem;
      color: var(--color-text-muted);
      font-size: 0.95rem;
      pointer-events: none;
      transition: all 0.3s ease;
  }

  /* Logged In Badge */
  .logged-in-badge {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem;
    background: rgba(139, 92, 246, 0.1);
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 12px;
    margin-bottom: 0.5rem;
  }

  .badge-avatar {
    width: 32px;
    height: 32px;
    flex-shrink: 0;
  }

  .badge-avatar img {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
  }

  .avatar-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, var(--color-primary), #ec4899);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: bold;
    font-size: 0.8rem;
  }

  .badge-info {
    display: flex;
    flex-direction: column;
    justify-content: center;
    line-height: 1.2;
  }

  .badge-label {
    font-size: 0.7rem;
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .badge-name {
    color: var(--color-text);
    font-size: 0.9rem;
  }

  /* Floating Label Logic */
  .input-wrapper input:focus ~ label,
  .input-wrapper input:not(:placeholder-shown) ~ label,
  .input-wrapper textarea:focus ~ label,
  .input-wrapper textarea:not(:placeholder-shown) ~ label {
      top: 0.25rem;
      font-size: 0.7rem;
      color: var(--color-primary);
      font-weight: 600;
      text-transform: uppercase;
  }
  
  .input-wrapper input:focus,
  .input-wrapper textarea:focus {
      border-color: var(--color-primary);
      background: rgba(139, 92, 246, 0.05);
      outline: none;
  }

  .submit-btn {
      background: #fff;
      color: #000;
      border: none;
      padding: 0.8rem 1.5rem;
      border-radius: 99px;
      font-weight: 700;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      cursor: pointer;
      transition: transform 0.2s, background 0.2s;
      width: fit-content;
  }
  
  [data-theme="light"] .submit-btn {
      background: #000;
      color: #fff;
  }

  .submit-btn:hover {
      transform: scale(1.05);
      background: #f0f0f0;
  }

  /* FEED AREA (Right) */
  .feed-list {
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
  }

  .feed-item {
      display: flex;
      gap: 1rem;
      animation: fadeIn 0.4s ease-out forwards;
      padding: 1.25rem;
      background: rgba(255,255,255,0.02);
      border-radius: 16px;
      border: 1px solid rgba(255,255,255,0.06);
      transition: all 0.3s ease;
      position: relative;
  }

  .feed-item::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 3px;
      background: linear-gradient(180deg, var(--color-primary) 0%, #ec4899 100%);
      border-radius: 3px 0 0 3px;
      opacity: 0;
      transition: opacity 0.3s ease;
  }

  .feed-item:hover {
      background: rgba(255,255,255,0.04);
      border-color: rgba(255,255,255,0.1);
      transform: translateX(4px);
  }

  .feed-item:hover::before {
      opacity: 1;
  }

  [data-theme="light"] .feed-item {
      background: #fff;
      border-color: rgba(0,0,0,0.06);
      box-shadow: 0 2px 8px rgba(0,0,0,0.04);
  }

  [data-theme="light"] .feed-item:hover {
      background: #fff;
      border-color: rgba(0,0,0,0.1);
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
  }

  @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
  }

  .item-avatar {
      flex-shrink: 0;
  }

  .item-avatar img {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      object-fit: cover;
      background: linear-gradient(135deg, #2a2a2a 0%, #1a1a1a 100%);
      border: 2px solid rgba(255,255,255,0.1);
      transition: transform 0.3s ease;
  }

  .feed-item:hover .item-avatar img {
      transform: scale(1.05);
  }

  [data-theme="light"] .item-avatar img {
      border-color: rgba(0,0,0,0.08);
      background: #e5e5e5;
  }

  .item-content {
      flex: 1;
      min-width: 0;
  }

  .item-header {
      display: flex;
      justify-content: space-between;
      margin-bottom: 0.75rem;
      align-items: flex-start;
      gap: 0.5rem;
  }

  .author-row {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 0.5rem;
  }
  
  .header-right {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      flex-shrink: 0;
  }

  .delete-btn {
      background: transparent;
      border: none;
      padding: 6px;
      color: var(--color-text-muted);
      cursor: pointer;
      opacity: 0; 
      transition: all 0.2s;
      border-radius: 6px;
  }
  
  .feed-item:hover .delete-btn {
      opacity: 0.5;
  }
  
  .delete-btn:hover {
      opacity: 1 !important;
      color: #ef4444;
      background: rgba(239, 68, 68, 0.1);
  }

  .item-author {
      font-weight: 700;
      color: var(--color-text-main);
      font-size: 0.95rem;
      letter-spacing: -0.01em;
  }

  .inline-rating {
      display: flex;
      gap: 1px;
  }

  .inline-rating .display-star {
      color: rgba(255, 255, 255, 0.15);
  }

  .inline-rating .display-star.filled {
      color: #f59e0b;
  }

  .inline-rating .display-star svg {
      width: 14px;
      height: 14px;
  }

  [data-theme="light"] .inline-rating .display-star {
      color: rgba(0, 0, 0, 0.1);
  }

  .item-date {
      font-size: 0.75rem;
      color: var(--color-text-muted);
      background: rgba(255,255,255,0.05);
      padding: 0.25rem 0.6rem;
      border-radius: 99px;
      font-weight: 500;
  }

  [data-theme="light"] .item-date {
      background: rgba(0,0,0,0.04);
  }

  .item-body {
      color: var(--color-text-dim);
      font-size: 0.95rem;
      line-height: 1.7;
      white-space: pre-wrap;
      word-break: break-word;
  }

  /* Empty State */
  .empty-feed {
      text-align: center;
      padding: 4rem 2rem;
      background: rgba(255,255,255,0.02);
      border-radius: 16px;
      border: 1px dashed rgba(255,255,255,0.1);
  }

  .empty-icon { font-size: 3rem; margin-bottom: 1rem; opacity: 0.5; }
  .empty-feed h4 { margin: 0 0 0.5rem; font-size: 1.1rem; }
  .empty-feed p { margin: 0; color: var(--color-text-muted); font-size: 0.9rem; }

  /* TOAST SYSTEM */
  .toast-container {
      position: fixed;
      bottom: 2rem;
      right: 2rem;
      z-index: 9999;
      display: flex;
      flex-direction: column;
      gap: 1rem;
      pointer-events: none;
  }

  .toast {
      pointer-events: auto;
      background: rgba(15, 15, 20, 0.95);
      border: 1px solid rgba(255, 255, 255, 0.1);
      padding: 1rem 1.5rem;
      border-radius: 12px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      min-width: 300px;
      opacity: 0;
      transform: translateY(10px);
      transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
  }
  
  [data-theme="light"] .toast {
      background: rgba(255, 255, 255, 0.95);
      border-color: rgba(0,0,0,0.1);
      box-shadow: 0 8px 32px rgba(0,0,0,0.1);
      color: #000;
  }

  .toast-content {
      display: flex;
      align-items: center;
      gap: 1rem;
  }

  .toast-icon {
      font-size: 1.25rem;
  }

  .toast-message {
      margin: 0;
      font-size: 0.95rem;
      font-weight: 500;
  }

  /* Variations */
  .toast-success { border-left: 4px solid #22c55e; }
  .toast-error { border-left: 4px solid #ef4444; }
  .toast-info { border-left: 4px solid var(--color-primary); }

  @media (max-width: 640px) {
      .toast-container {
          bottom: 1rem;
          right: 1rem;
          left: 1rem;
      }
      .toast {
          min-width: auto;
          width: 100%;
      }
  }

  /* STAR RATING SYSTEM */
  .rating-wrapper {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
  }

  .rating-label {
      font-size: 0.85rem;
      color: var(--color-text-muted);
      font-weight: 500;
  }

  .star-rating {
      display: flex;
      gap: 0.25rem;
  }

  .star-rating .star {
      background: transparent;
      border: none;
      padding: 4px;
      cursor: pointer;
      color: rgba(255, 255, 255, 0.2);
      transition: all 0.2s ease;
      border-radius: 4px;
  }

  [data-theme="light"] .star-rating .star {
      color: rgba(0, 0, 0, 0.15);
  }

  .star-rating .star svg {
      width: 24px;
      height: 24px;
      display: block;
      transition: transform 0.2s ease;
  }

  .star-rating .star:hover svg {
      transform: scale(1.15);
  }

  .star-rating .star.hover,
  .star-rating .star.active {
      color: #fbbf24;
  }

  .star-rating .star.active {
      color: #f59e0b;
  }

  /* Display stars in comments - handled by .inline-rating and .display-star */
  .display-star {
      color: rgba(255, 255, 255, 0.15);
      display: flex;
  }

  [data-theme="light"] .display-star {
      color: rgba(0, 0, 0, 0.1);
  }

  .display-star.filled {
      color: #f59e0b;
  }

  .display-star svg {
      width: 16px;
      height: 16px;
  }
</style>
