---
import Layout from '../layouts/Layout.astro';

const team = [
  { 
    name: 'Abel Fernández', 
    role: 'Guionista', 
    image: 'https://iili.io/fzO4DGV.jpg',
    link: 'https://www.instagram.com/abelfrp/'
  },
  { 
    name: 'Pablo Luna', 
    role: 'Web, Sonido y Programación', 
    link: 'https://broslunas.com', 
    image: 'https://i.imgur.com/G4IX2d0.jpeg' 
  },
  { 
    name: 'Pablo Santamaría', 
    role: 'Diseño Web y Sonido', 
    image: 'https://iili.io/fzetcMu.jpg',
    link: 'https://www.instagram.com/pabl0.sp/',
  },
  { 
    name: 'Omar Reyes', 
    role: 'Diseñador Web', 
    image: 'https://iili.io/fze34sV.jpg',
    link: 'https://www.instagram.com/ereyes_magos/'
  },
  { 
    name: 'Dylan Jorge', 
    role: 'Redactor Jefe', 
    image: 'https://iili.io/fzObDbI.jpg' 
  },
  { 
    name: 'Pablo Pérez', 
    role: 'Integrante del Podcast', 
    image: 'https://iili.io/fze0awJ.jpg',
    link: 'https://www.instagram.com/__pabloprz/'
  },
  { 
    name: 'Miguel Salazar', 
    role: 'Estrategia Digital', 
    image: 'https://iili.io/f8u8VQS.jpg',
    link: 'https://www.instagram.com/miguelslzzz/',
  },
];
---

<Layout title="Sobre Nosotros">
    <div class="container about-page">
        <!-- Hero Section -->
        <section class="intro fade-in-up">
            <span class="badge">El Equipo</span>
            <h1>Quiénes Hacemos <br><span class="text-gradient">Veredillas FM</span></h1>
            <p class="big-text">
                Un proyecto impulsado por la creatividad y el talento de los estudiantes de 2º de Bachillerato B.
            </p>
        </section>

        <!-- Mission Grid -->
        <section class="mission fade-in-up delay-100">
            <div class="mission-grid">
                <div class="card glass-panel">
                    <div class="icon-box">🎙️</div>
                    <h3>Nuestra Misión</h3>
                    <p>Crear un espacio dinámico y abierto para la libre expresión, donde cada estudiante tenga una voz y cada historia merezca ser contada.</p>
                </div>
                <div class="card glass-panel">
                    <div class="icon-box">👁️</div>
                    <h3>Nuestra Visión</h3>
                    <p>Ser el referente de comunicación digital del instituto, demostrando que la tecnología y la creatividad pueden transformar la educación.</p>
                </div>
                <div class="card glass-panel">
                    <div class="icon-box">⚡</div>
                    <h3>Nuestra Pasión</h3>
                    <p>Nos mueve la curiosidad. Desde salseos del insti hasta debates profundos, lo cubrimos todo con un estilo único.</p>
                </div>
            </div>
        </section>

        <!-- Team Section -->
        <section class="team fade-in-up delay-200">
            <div class="section-header">
                <h2>Conoce a los creadores</h2>
                <p>Las mentes detrás del micrófono y los controles</p>
            </div>
            
            <div class="team-grid">
                {team.map((member) => (
                    <div class="member-card group">
                        <div class="image-wrapper">
                            <img src={member.image} alt={member.name} class="member-image" loading="lazy" />
                            <div class="overlay"></div>
                        </div>
                        <div class="member-info">
                            <h3>
                                {member.link ? (
                                    <a href={member.link} target="_blank" rel="noopener noreferrer" class="member-link">
                                        {member.name}
                                        <span class="link-icon">↗</span>
                                    </a>
                                ) : (
                                    member.name
                                )}
                            </h3>
                            <span class="role-badge">{member.role}</span>
                        </div>
                    </div>
                ))}
            </div>
        </section>
    </div>
</Layout>

<style>
    .about-page {
        padding: 4rem var(--spacing-md);
        max-width: 1000px; /* Constrain width for better centering appearance */
        margin: 0 auto;
    }

    /* Intro */
    .intro {
        text-align: center;
        max-width: 800px;
        margin: 0 auto 6rem;
    }

    .badge {
        display: inline-block;
        padding: 0.5rem 1.5rem;
        border-radius: 50px;
        background: rgba(139, 92, 246, 0.1);
        color: var(--color-primary);
        font-weight: 600;
        margin-bottom: 1.5rem;
        border: 1px solid rgba(139, 92, 246, 0.2);
    }

    .intro h1 {
        font-size: clamp(3rem, 5vw, 4.5rem);
        line-height: 1.1;
        margin-bottom: 1.5rem;
        letter-spacing: -0.02em;
    }

    .big-text {
        font-size: 1.25rem;
        color: var(--color-text-muted);
        line-height: 1.6;
        max-width: 600px;
        margin: 0 auto;
    }

    /* Mission Cards */
    .mission-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-bottom: 8rem;
    }

    .card {
        padding: 2.5rem;
        border-radius: 24px;
        transition: transform 0.3s ease, border-color 0.3s ease;
        position: relative;
        overflow: hidden;
        text-align: center;
    }
    
    .card:hover {
        transform: translateY(-5px);
        border-color: rgba(139, 92, 246, 0.3);
    }

    .icon-box {
        width: 60px;
        height: 60px;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        margin: 0 auto 1.5rem;
    }

    .card h3 {
        font-size: 1.5rem;
        margin-bottom: 1rem;
        color: var(--color-text-main);
    }

    .card p {
        color: var(--color-text-muted);
        line-height: 1.6;
    }

    /* Team Grid */
    .section-header {
        text-align: center;
        margin-bottom: 4rem;
    }

    .section-header h2 {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
    }

    .section-header p {
        color: var(--color-text-muted);
        font-size: 1.1rem;
    }

    .team-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
        gap: 2rem;
        justify-content: center;
    }

    .member-card {
        position: relative;
        max-width: 220px;
        margin: 0 auto;
    }

    .image-wrapper {
        aspect-ratio: 1;
        border-radius: 100px; /* Circle for a more pro look? Or just very rounded? Let's stick to rounded square but smaller. rounded-xl is usually 12-16px. Let's do 16px */
        border-radius: 16px;
        overflow: hidden;
        margin-bottom: 1rem;
        position: relative;
        background: var(--color-surface-hover);
        box-shadow: 0 8px 24px -8px rgba(0,0,0,0.4);
    }

    .member-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: all 0.4s ease;
    }

    .member-card:hover .member-image {
        transform: scale(1.05);
    }

    .member-info {
        text-align: center;
    }

    .member-info h3 {
        font-size: 1.25rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
    }

    .member-link {
        display: inline-flex;
        align-items: center;
        gap: 4px;
        transition: color 0.2s;
    }

    .member-link:hover {
        color: var(--color-primary);
    }

    .link-icon {
        font-size: 0.8em;
        opacity: 0.5;
    }

    .role-badge {
        display: inline-block;
        font-size: 0.875rem;
        color: var(--color-text-muted);
        padding: 0.25rem 1rem;
        border: 1px solid var(--color-border);
        border-radius: 100px;
        background: rgba(255, 255, 255, 0.02);
    }

    /* Animations */
    .fade-in-up {
        animation: fadeInUp 0.8s ease-out forwards;
        opacity: 0;
        transform: translateY(20px);
    }
    
    .delay-100 { animation-delay: 0.1s; }
    .delay-200 { animation-delay: 0.2s; }

    @keyframes fadeInUp {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>
