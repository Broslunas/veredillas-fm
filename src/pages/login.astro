---
export const prerender = false;
import Layout from '../layouts/Layout.astro';
import { getUserFromCookie } from '../lib/auth';

const cookieHeader = Astro.request.headers.get('cookie');
const userPayload = getUserFromCookie(cookieHeader);

if (userPayload) {
    return Astro.redirect('/dashboard');
}

const errorParam = Astro.url.searchParams.get('error');
let errorMessage = '';
if (errorParam === 'InvalidToken') {
    errorMessage = 'El enlace de acceso es inválido o está mal formado.';
} else if (errorParam === 'ExpiredOrInvalid') {
    errorMessage = 'El enlace de acceso ha expirado u ocurrido un error interno. Intenta de nuevo.';
} else if (errorParam) {
    errorMessage = 'Ocurrió un error al iniciar sesión. Intenta de nuevo.';
}
---

<Layout 
    title="Iniciar Sesión" 
    description="Únete a la comunidad de Veredillas FM. Guarda tus favoritos, sube de nivel y compite en el ranking de oyentes."
    robots="noindex, nofollow"
>
    <!-- Background Decor -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none -z-10">
        <div class="blob blob-1"></div>
        <div class="blob blob-2"></div>
        <div class="grid-overlay"></div>
    </div>

    <div class="container login-container">
        <div class="login-wrapper gs-fade-up">
            
            <div class="login-left-col">
                <div class="login-header">
                    <div class="logo-circle">
                        <img src="/logo.webp" alt="Veredillas FM Logo" />
                    </div>
                    <h1>Únete a Veredillas FM</h1>
                    <p>Más que un podcast, una comunidad. Accede a funciones exclusivas y lleva tu experiencia al siguiente nivel.</p>
                </div>

                <div class="features-grid">
                    <div class="feature-item">
                        <span class="feature-icon">⭐</span>
                        <div class="feature-text">
                            <h3>Guarda tus Favoritos</h3>
                            <p>No pierdas de vista los episodios que más te gustan.</p>
                        </div>
                    </div>
                    
                    <div class="feature-item">
                        <span class="feature-icon">🏆</span>
                        <div class="feature-text">
                            <h3>Sube de Nivel</h3>
                            <p>Escucha hasta convertirte en una <strong>Leyenda</strong>.</p>
                        </div>
                    </div>

                    <div class="feature-item">
                        <span class="feature-icon">📊</span>
                        <div class="feature-text">
                            <h3>Tu Dashboard</h3>
                            <p>Estadísticas detalladas de tu tiempo de escucha.</p>
                        </div>
                    </div>

                    <div class="feature-item">
                        <span class="feature-icon">👑</span>
                        <div class="feature-text">
                            <h3>Top Oyentes</h3>
                            <p>Compite en el widget de Top Oyentes.</p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="login-right-col">
                <div class="login-card">
                    <h2>Bienvenido</h2>
                    <p class="login-sub">Inicia sesión para continuar</p>
                    
                    {errorMessage && (
                        <div class="login-error-banner" style="background: rgba(239, 68, 68, 0.1); color: #ef4444; border: 1px solid rgba(239, 68, 68, 0.2); padding: 0.75rem; border-radius: 0.5rem; margin-bottom: 1.5rem; font-size: 0.9rem;">
                            {errorMessage}
                        </div>
                    )}
                    
                    <a href="/api/auth/google/login" class="btn btn-google btn-xl">
                        <svg width="24" height="24" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                            <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" fill="#4285F4"/>
                            <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/>
                            <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" fill="#FBBC05"/>
                            <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/>
                        </svg>
                        Continuar con Google
                    </a>
                    
                    <div class="divider">
                        <span>o entra con tu correo</span>
                    </div>

                    <form id="magic-link-form" class="magic-link-form">
                        <div class="input-group">
                            <input type="email" id="email-input" name="email" placeholder="tu@correo.com" required />
                        </div>
                        <button type="submit" class="btn btn-primary btn-xl magic-submit-btn">
                            Enviar Enlace Mágico
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-left: 0.5rem;"><path d="M22 2 11 13"/><path d="m22 2-7 20-4-9-9-4Z"/></svg>
                        </button>
                    </form>
                    
                    <div id="magic-link-message" class="magic-link-message" style="display: none;"></div>

                    <p class="terms-note">
                        Al iniciar sesión, aceptas unirte a nuestra newsletter, nuestros <a href="/terminos-y-condiciones">Términos</a> y <a href="/politica-de-privacidad">Política de Privacidad</a>.
                    </p>
                </div>
            </div>
        </div>
    </div>
</Layout>

<style>
    .login-container {
        min-height: 90vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding-top: 2rem;
        padding-bottom: 2rem;
    }

    .login-wrapper {
        width: 100%;
        max-width: 1100px; /* Wider for 2 cols */
        background: rgba(15, 15, 20, 0.6);
        border: 1px solid rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        padding: 4rem;
        border-radius: 2rem;
        position: relative;
        overflow: hidden;
        
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        align-items: center;
    }

    /* Mobile */
    @media (max-width: 900px) {
        .login-wrapper {
            grid-template-columns: 1fr;
            text-align: center;
            padding: 2rem;
            gap: 3rem;
        }
        
        .login-left-col {
            padding-right: 0;
            border-right: none;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            padding-bottom: 2rem;
        }
        
        .login-header .logo-circle {
            margin-left: auto;
            margin-right: auto;
        }
    }

    /* Top decorative glow */
    .login-wrapper::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 1px;
        background: linear-gradient(90deg, transparent, var(--color-primary), transparent);
        opacity: 0.5;
    }

    /* Left Column */
    .login-left-col {
        padding-right: 2rem;
    }

    .login-header {
        margin-bottom: 2.5rem;
    }

    .logo-circle {
        width: 60px;
        height: 60px;
        margin-bottom: 1.5rem;
        background: black;
        border-radius: 50%;
        padding: 5px;
        border: 2px solid rgba(255, 255, 255, 0.1);
        box-shadow: 0 0 30px rgba(139, 92, 246, 0.3);
    }

    .logo-circle img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 50%;
    }

    h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        line-height: 1.1;
        background: linear-gradient(to right, #fff, #a1a1aa);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .login-header p {
        color: var(--color-text-muted);
        font-size: 1.1rem;
        line-height: 1.6;
    }

    .features-grid {
        display: grid;
        grid-template-columns: 1fr 1fr; /* Always 2 cols on desktop/tablet */
        gap: 1.5rem;
        text-align: left;
    }
    
    @media (max-width: 640px) {
        .features-grid {
            grid-template-columns: 1fr; /* Stack on small mobile */
        }
    }

    .feature-item {
        background: rgba(255, 255, 255, 0.03);
        padding: 1rem;
        border-radius: 1rem;
        display: flex;
        flex-direction: column; /* Stack icon top */
        gap: 0.75rem;
        align-items: flex-start;
        border: 1px solid transparent;
        transition: all 0.2s;
    }
    
    .feature-item:hover {
        background: rgba(255, 255, 255, 0.06);
        border-color: rgba(255, 255, 255, 0.1);
    }

    .feature-icon {
        font-size: 1.25rem;
        background: rgba(255, 255, 255, 0.05);
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 0.5rem;
    }

    .feature-text h3 {
        font-size: 0.95rem;
        margin-bottom: 0.25rem;
        color: var(--color-text-main);
    }

    .feature-text p {
        font-size: 0.8rem;
        color: var(--color-text-muted);
        line-height: 1.4;
    }

    /* Right Column - Login Card */
    .login-right-col {
        display: flex;
        justify-content: center;
        align-items: center;
    }
    
    .login-card {
        background: rgba(0,0,0,0.3);
        padding: 2.5rem;
        border-radius: 1.5rem;
        border: 1px solid rgba(255,255,255,0.05);
        width: 100%;
        max-width: 400px;
        text-align: center;
    }

    .login-card h2 {
        font-size: 1.75rem;
        margin-bottom: 0.5rem;
    }
    
    .login-sub {
        color: var(--color-text-muted);
        margin-bottom: 2rem;
    }

    .btn-google {
        background: white;
        color: #1f1f1f;
        border: none;
        width: 100%;
        margin: 0 auto;
        gap: 0.75rem;
        font-size: 1rem;
        padding: 1rem;
        transition: transform 0.2s, box-shadow 0.2s;
    }

    .btn-google:hover {
        background: #f8f9fa;
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    }

    .divider {
        display: flex;
        align-items: center;
        text-align: center;
        margin: 1.5rem 0;
        color: var(--color-text-dim);
        font-size: 0.85rem;
    }
    
    .divider::before,
    .divider::after {
        content: '';
        flex: 1;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .divider not(:empty)::before {
        margin-right: 1rem;
    }
    
    .divider not(:empty)::after {
        margin-left: 1rem;
    }

    .magic-link-form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .input-group input {
        width: 100%;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 0.5rem;
        color: white;
        font-size: 1rem;
        transition: all 0.2s;
        box-sizing: border-box;
    }

    .input-group input:focus {
        outline: none;
        border-color: var(--color-primary);
        background: rgba(255, 255, 255, 0.1);
    }

    .magic-submit-btn {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        padding: 1rem;
        font-size: 1rem;
    }
    
    .magic-link-message {
        margin-top: 1rem;
        padding: 0.75rem;
        border-radius: 0.5rem;
        font-size: 0.9rem;
        line-height: 1.4;
    }

    .magic-link-message.success {
        background: rgba(16, 185, 129, 0.1);
        color: #10b981;
        border: 1px solid rgba(16, 185, 129, 0.2);
    }

    .magic-link-message.error {
        background: rgba(239, 68, 68, 0.1);
        color: #ef4444;
        border: 1px solid rgba(239, 68, 68, 0.2);
    }

    .terms-note {
        margin-top: 1.5rem;
        font-size: 0.8rem;
        color: var(--color-text-dim);
    }

    .terms-note a {
        color: var(--color-text-muted);
        text-decoration: underline;
    }
    
    .terms-note a:hover {
        color: var(--color-primary);
    }

    /* Background blobs */
    .blob {
        position: absolute;
        width: 500px;
        height: 500px;
        border-radius: 50%;
        filter: blur(80px);
        opacity: 0.3;
    }
    
    .blob-1 {
        top: -100px;
        left: -100px;
        background: radial-gradient(circle, var(--color-primary), transparent);
        animation: blob-float 10s infinite alternate;
    }
    
    .blob-2 {
        bottom: -100px;
        right: -100px;
        background: radial-gradient(circle, var(--color-secondary), transparent);
        animation: blob-float 12s infinite alternate-reverse;
    }

    @keyframes blob-float {
        0% { transform: translate(0, 0); }
        100% { transform: translate(50px, 50px); }
    }
</style>

<script>
    function openGoogleLogin(e: Event) {
        e.preventDefault();
        const width = 500;
        const height = 600;
        const left = (window.innerWidth - width) / 2 + window.screenLeft;
        const top = (window.innerHeight - height) / 2 + window.screenTop;
        
        window.open(
            '/api/auth/google/login',
            'google_login_popup',
            `width=${width},height=${height},top=${top},left=${left},status=no,menubar=no,toolbar=no`
        );
    }

    // Attach event listener safely
    document.addEventListener('DOMContentLoaded', () => {
        const btn = document.querySelector('.btn-google');
        if (btn) {
            btn.addEventListener('click', openGoogleLogin);
        }

        const magicForm = document.getElementById('magic-link-form') as HTMLFormElement;
        const magicMessage = document.getElementById('magic-link-message');
        const submitBtn = magicForm?.querySelector('button[type="submit"]') as HTMLButtonElement;

        if (magicForm && magicMessage && submitBtn) {
            magicForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                
                // Reset state
                magicMessage.style.display = 'none';
                magicMessage.className = 'magic-link-message';
                const originalBtnText = submitBtn.innerHTML;
                
                // Loading state
                submitBtn.disabled = true;
                submitBtn.innerHTML = 'Enviando enlace...';

                try {
                    const formData = new FormData(magicForm);
                    const response = await fetch('/api/auth/email/send-magic-link', {
                        method: 'POST',
                        body: formData
                    });

                    const data = await response.json();

                    magicMessage.style.display = 'block';
                    if (response.ok && data.success) {
                        magicMessage.classList.add('success');
                        magicMessage.textContent = '¡Enlace enviado! Revisa tu bandeja de entrada (y la carpeta de spam).';
                        magicForm.reset();
                    } else {
                        throw new Error(data.error || 'Error al enviar el enlace');
                    }
                } catch (error: any) {
                    magicMessage.style.display = 'block';
                    magicMessage.classList.add('error');
                    magicMessage.textContent = error.message || 'Ha ocurrido un error inesperado.';
                } finally {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = originalBtnText;
                }
            });
        }
    });
</script>
